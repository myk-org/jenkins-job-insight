# Jenkins Job Insight - Environment Variables Template

# ===================
# Jenkins Configuration (Required)
# ===================
JENKINS_URL=https://jenkins.example.com
JENKINS_USER=your-username
JENKINS_PASSWORD=your-api-token
JENKINS_SSL_VERIFY=true

# ===================
# AI CLI Configuration
# ===================
# Choose AI provider: "claude", "gemini", "cursor", or "qodo"
AI_PROVIDER=claude

# --- Claude CLI Options ---

# Option 1: Direct API key (simplest)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Option 2: Vertex AI authentication
# CLAUDE_CODE_USE_VERTEX=1
# CLOUD_ML_REGION=us-east5
# ANTHROPIC_VERTEX_PROJECT_ID=your-project-id
# ANTHROPIC_MODEL=claude-sonnet-4-5@20251101
# ANTHROPIC_SMALL_FAST_MODEL=claude-haiku-4-5@20251001

# --- Gemini CLI Options ---

# Option 1: API key
GEMINI_API_KEY=your-gemini-api-key

# Option 2: OAuth (run: gemini auth login)
# No env vars needed for OAuth

# --- Cursor Agent CLI Options ---

# Choose ONE of the following authentication methods:

# Option 1: API key (set this environment variable)
# CURSOR_API_KEY=your-cursor-api-key

# Option 2: Auth file mount (for users who authenticated via `agent login`)
# Mount ~/.config/cursor/auth.json to /home/appuser/.config/cursor/auth.json
# No env vars needed for this method

# Optional: Specify the model for Cursor Agent
# If not set, Cursor uses its default model
# CURSOR_MODEL=claude-3.5-sonnet

# --- Qodo CLI Options ---

# API key (required for Qodo)
# QODO_API_KEY=your-qodo-api-key

# Optional: Specify the model for Qodo
# Available models: claude-4.5-sonnet, gemini-2.5-pro, gpt-5.2, grok-4, etc.
# If not set, Qodo uses its default model
# QODO_MODEL=claude-4.5-sonnet

# --- AI CLI Timeout ---

# Timeout for AI CLI calls in minutes (default: 10)
# Increase for slower models like gpt-5.2
# AI_CLI_TIMEOUT=10

# ===================
# Logging Configuration
# ===================
# Log level: DEBUG, INFO, WARNING, ERROR (default: INFO)
LOG_LEVEL=INFO

# ===================
# Optional Defaults (can be overridden per-request in webhook)
# ===================

# Tests repository URL
# TESTS_REPO_URL=https://github.com/org/test-repo

# Callback webhook URL for async results
# CALLBACK_URL=https://your-service.com/webhook/results

# Callback headers (JSON format)
# CALLBACK_HEADERS={"Authorization": "Bearer xxx"}

# Slack webhook URL
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx

# ===================
# Custom Prompt
# ===================

# Path to custom analysis prompt file
# Mount your own PROMPT.md to customize AI analysis
# PROMPT_FILE=/app/PROMPT.md

# ===================
# Development
# ===================

# Enable debug mode
DEBUG=false
